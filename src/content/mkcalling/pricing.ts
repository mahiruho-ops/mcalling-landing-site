/**
 * Primary marketing copy for `/pricing` lives in `pricingPage.ts` (`pricingPageContent`).
 * `/pricing-old` permanently redirects to `/pricing` (see `next.config.ts`).
 */

export { pricingPageContent } from "./pricingPage";
